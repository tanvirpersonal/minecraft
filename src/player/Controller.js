import*as THREE from "three";import{PointerLockControls}from "three/addons/controls/PointerLockControls.js";export class Controller{constructor(camera,dom){this.controls=new PointerLockControls(camera,dom);this.keys=new Set;this.moveX=0;this.moveZ=0;this.jump=false;this.sprint=false;this.forward=new THREE.Vector3;this.right=new THREE.Vector3;addEventListener("keydown",e=>{this.keys.add(e.code);if(["Space","KeyW","KeyA","KeyS","ShiftLeft","ShiftRight"].includes(e.code))e.preventDefault()});addEventListener("keyup",e=>this.keys.delete(e.code));this.controls.addEventListener("unlock",()=>this.onUnlock?.());this.controls.addEventListener("lock",()=>this.onLock?.())}updateVectors(){const c=this.controls.object;c.getWorldDirection(this.forward);this.forward.y=0;if(this.forward.lengthSq()<1e-6)this.forward.set(0,0,-1);this.forward.normalize();this.right.crossVectors(this.forward,new THREE.Vector3(0,1,0)).normalize();this.moveZ=(this.keys.has("KeyW")?1:0)+(this.keys.has("KeyS")?-1:0);this.moveX=(this.keys.has("KeyD")?1:0)+(this.keys.has("KeyA")?-1:0);this.jump=this.keys.has("Space");this.sprint=this.keys.has("ShiftLeft")||this.keys.has("ShiftRight")}lock(){this.controls.lock()}isLocked(){return this.controls.isLocked}}