import*as THREE from "three";import{Physics}from "./Physics.js";export class Player{constructor(world,camera){this.world=world;this.camera=camera;this.position=camera.position;this.velocity=new THREE.Vector3;this.physics=new Physics(world)}spawnAt(x,z){for(let y=63;y>=1;y--){const id=this.world.getBlock(Math.floor(x),y,Math.floor(z));if(id!==0&&id!==13){this.position.set(x+.5,y+1.62,z+.5);this.velocity.set(0,0,0);return}}this.position.set(0,40,0)}update(dt,c){c.updateVectors();this.physics.update(this.position,this.velocity,dt,c)}}