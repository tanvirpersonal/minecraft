export class Inventory{constructor(size=36){this.slots=Array.from({length:size},()=>({id:0,count:0}))}add(id,count=1){let left=count;for(const s of this.slots)if(s.id===id&&s.count<64){const take=Math.min(64-s.count,left);s.count+=take;left-=take;if(!left)return true}for(const s of this.slots)if(s.count===0){const take=Math.min(64,left);s.id=id;s.count=take;left-=take;if(!left)return true}return left===0}count(id){return this.slots.reduce((n,s)=>n+(s.id===id?s.count:0),0)}remove(id,count=1){let left=count;for(const s of this.slots)if(s.id===id){const take=Math.min(s.count,left);s.count-=take;left-=take;if(s.count===0)s.id=0;if(!left)return true}return false}ensureHotbar(ids){ids.forEach((id,i)=>{if(this.slots[i].count===0){this.slots[i].id=id;this.slots[i].count=64}})}}