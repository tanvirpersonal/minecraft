import*as THREE from "three";export class LightingSystem{constructor(r,t){this.r=r;this.t=t;this.day=new THREE.Color(0x86bfd3);this.night=new THREE.Color(0x0a1324)}update(){const d=this.t.daylight(),a=this.t.time*Math.PI*2;this.r.sun.position.set(Math.cos(a)*55,Math.sin(a)*70,25);this.r.sun.intensity=.35+d*2;this.r.hemi.intensity=.45+d*1.5;const c=new THREE.Color().copy(this.night).lerp(this.day,d);this.r.scene.background.copy(c);this.r.scene.fog.color.copy(c)}}