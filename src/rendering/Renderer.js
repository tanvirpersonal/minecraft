import*as THREE from "three";export class Renderer{constructor(container){this.scene=new THREE.Scene;this.scene.background=new THREE.Color(0x86bfd3);this.scene.fog=new THREE.Fog(0x86bfd3,38,135);this.camera=new THREE.PerspectiveCamera(74,innerWidth/innerHeight,.05,220);this.camera.position.set(0,30,0);this.renderer=new THREE.WebGLRenderer({antialias:false,powerPreference:"high-performance"});this.renderer.setPixelRatio(Math.min(devicePixelRatio,1.75));this.renderer.setSize(innerWidth,innerHeight);this.renderer.outputColorSpace=THREE.SRGBColorSpace;container.appendChild(this.renderer.domElement);this.hemi=new THREE.HemisphereLight(0xbfe7ff,0x4b4a36,1.8);this.sun=new THREE.DirectionalLight(0xffefc6,2);this.sun.position.set(-40,70,20);this.scene.add(this.hemi,this.sun);addEventListener("resize",()=>this.resize())}resize(){this.camera.aspect=innerWidth/innerHeight;this.camera.updateProjectionMatrix();this.renderer.setSize(innerWidth,innerHeight)}render(){this.renderer.render(this.scene,this.camera)}}